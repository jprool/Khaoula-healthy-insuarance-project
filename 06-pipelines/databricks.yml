# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "khaoula-healthy-insurance-project"

include:
  - resources/*.yml
  - resources/*/*.yml

sync:
  paths:
    - .
    - ../03-silver-transformations
    - ../05-gold-transformations
    
targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405611693577244.4.azuredatabricks.net

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://adb-7405611693577244.4.azuredatabricks.net
      # We always use /Users/jprool12@gmail.com for all resources to make sure we only have a single copy.
      root_path: /Users/jprool12@gmail.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as jprool12@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: jprool12@gmail.com
